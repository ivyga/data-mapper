const services = require("./services");
const { getCars, getPeople } = services;

function datamapper() {
  this.getPersonsCars = async function getPersonsCars(personId) {
    /**
     * Complete this function to make the tests pass
     */
  };
}

module.exports = datamapper;
